#include "stdafx.h"
#include "NormalMissileGenInfo.h"
#include "CommonNormalMissileInfo.h"
#include "SpecifiedNormalMissileInfo.h"
/*
void NormalMissileGenInfo::init()
{
	_commonMissileInfo = new CommonNormalMissileInfo();
	_specifiedMissileInfo = new SpecifiedNormalMissileInfo();

	_commonMissileInfo->init();
	_specifiedMissileInfo->init();
}

void NormalMissileGenInfo::init(std::string & commonMissileInfoFile, std::string & specifiedMissileInfo)
{
	if(commonMissileInfoFile.compare("") == 0)
		_commonMissileInfo->init();
	else
		_commonMissileInfo->init(commonMissileInfoFile);

	if (specifiedMissileInfo.compare("") == 0)
		_specifiedMissileInfo->init();
	else
		_specifiedMissileInfo->init(specifiedMissileInfo);
}

*/